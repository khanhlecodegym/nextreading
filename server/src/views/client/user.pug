extends _default/base.pug

block metadata
  - var metaPageTitle = seoTitle
  - var metaPageDesc = seoDesc
  - var metaContentType = "article"
  - var metaPageImage = img
  - var metaCanonical = `${BASE_URL}/user/${userProfile.username}`
  - var metaAMPLink = `${BASE_URL}/amp/user/${userProfile.username}`

block schemaMarkup
  include _schema/schema-user.pug

block main
  include _template/user.pug
  +userTemplate(false)
