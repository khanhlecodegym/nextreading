extends _default/base.pug

block metadata
  - var metaPageTitle = seoTitle
  - var metaPageDesc = seoDesc
  - var metaContentType = "article"
  - var metaPageImage = img
  - var metaCanonical = `${BASE_URL}/book/${slug}`
  - var metaAMPLink = `${BASE_URL}/amp/book/${slug}`

block schemaMarkup
  include ../_schema/schema-book.pug

block main
  include ../_template/book.pug
  +bookTemplate(true)
