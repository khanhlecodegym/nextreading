extends _default/base.pug

block metadata
  - var metaPageTitle = seoTitle
  - var metaPageDesc = seoDesc
  - var metaContentType = "article"
  - var metaPageImage = img
  - var metaCanonical = `${BASE_URL}/review/${reviewId}/${bookSlug}`
  - var metaAMPLink = `${BASE_URL}/amp/review/${reviewId}/${bookSlug}`

block schemaMarkup
  include ../_schema/schema-review.pug

block main
  include ../_template/review.pug
  +reviewDetailTemplate(true)
